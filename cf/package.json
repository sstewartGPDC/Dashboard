{
  "name": "gpdc-dashboard-cf",
  "private": true,
  "type": "module",
  "scripts": {
    "db:init": "wrangler d1 execute DB --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute DB --remote --file=./schema.sql",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
